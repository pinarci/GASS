from pathlib import Path

PROJECT_ROOT = Path(__file__).resolve().parent
KNOWN_FACES_DIR = PROJECT_ROOT / "static" / "known_faces"
UPLOAD_FOLDER = PROJECT_ROOT / "static" / "uploads"
ATTENDANCE_FILE = PROJECT_ROOT / "attendance.csv"
